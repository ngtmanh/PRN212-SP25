<Window x:Class="Prn212.ResetPassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prn212"
        mc:Ignorable="d"
        Title="ResetPassword" Height="400" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Background="#F9F9F9">
        <Border Width="350" Height="350" Background="White" CornerRadius="10"
        HorizontalAlignment="Center" VerticalAlignment="Center" 
        Padding="20" BorderBrush="DarkBlue" BorderThickness="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">


                <TextBlock Text="🔐 Đặt lại mật khẩu" FontSize="20" FontWeight="Bold" 
                   Foreground="#3498DB" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                    <TextBlock Text="📧Email" FontSize="18" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtEmail" Width="200" Height="30" Margin="10,0,0,0"
                      FontSize="14"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                    <TextBlock Text="🔑Mã OTP" FontSize="18" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtOtp" Width="166" Height="30" Margin="10,0,0,0"
                         FontSize="14"/>
                </StackPanel>


                <Button Content="➡ Gửi mã OTP" Width="250" Height="35" Margin="0,10,0,5" 
                Background="#3498DB" Foreground="White" FontSize="14" 
                Click="BtnSendOtp_Click" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="➡ Xác nhận mã OTP" Width="250" Height="35" Margin="0,10,0,5" 
                    Background="Brown" Foreground="White" FontSize="14" 
                    Click="BtnVerifyOtp_Click" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="➡ Quay lại" Width="250" Height="35" Margin="0,10,0,5" 
                Background="Gray" Foreground="White" FontSize="14"
                Click="BtnBack_Click" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
