<Window x:Class="Prn212.PoliceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prn212"
        mc:Ignorable="d"
        Title="Trang chủ" Height="400" Width="350" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.Background>
        <ImageBrush Stretch="Fill"/>
    </Window.Background>
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="Danh Mục" FontSize="20" FontWeight="Bold" 
       Foreground="Black" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Button Content="👤 Dữ liệu cá nhân" Width="200" Height="40" Margin="10"
                Foreground="White" Background="#3498DB" FontSize="14" Cursor="Hand"
                Click="BtnProfile_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Content="📄 Xem đơn duyệt" Width="200" Height="40" Margin="10"
                Foreground="White" Background="#2ECC71" FontSize="14" Cursor="Hand"
                Click="BtnViewRegistration_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Content="➡ Đăng xuất" Width="200" Height="40" Margin="10"
                Foreground="White" Background="#E74C3C" FontSize="14" Cursor="Hand"
                Click="BtnLogout_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </StackPanel>
    </Grid>

</Window>
