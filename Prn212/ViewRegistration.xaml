<Window x:Class="Prn212.ViewRegistration"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prn212"
        mc:Ignorable="d"
        Title="ViewRegistration" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" FontSize="18" FontWeight="Bold" Foreground="#007ACC"
                   Text="🏡 Hộ Gia Đình" VerticalAlignment="Center"/>

        <!-- Thông tin hộ gia đình -->
        <Grid Grid.Row="1" Margin="10" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Mã hộ -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="ⓘ Mã hộ:" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" Width="300" Margin="5"/>

            <!-- Địa chỉ -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="📍 Địa chỉ:" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1" Width="300" Margin="5"/>

            <!-- Ngày tạo -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="📅 Ngày tạo:" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" Width="300" Margin="5"/>

            <!-- Chủ hộ -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="👤 Chủ hộ:" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox Grid.Row="3" Grid.Column="1" Width="300" Margin="5"/>
        </Grid>

        <!-- Danh sách thành viên -->
        <StackPanel Grid.Row="2" Margin="10">
            <TextBlock FontSize="16" FontWeight="Bold" Foreground="#007ACC"
                       Text="👥 Danh Sách Thành Viên"/>

            <DataGrid x:Name="dgMembers" AutoGenerateColumns="False" Margin="0,10,0,0"
                      HeadersVisibility="Column" CanUserAddRows="False" 
                      RowHeight="30" BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="🆔 Công dân" Binding="{Binding CitizenId}" Width="*"/>
                    <DataGridTextColumn Header="👤 Tên Công dân" Binding="{Binding CitizenName}" Width="*"/>
                    <DataGridTextColumn Header="🔗 Mối quan hệ" Binding="{Binding Relationship}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
